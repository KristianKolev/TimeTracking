services:
  timetracking:
    build: .
    image: timetracking:latest
    container_name: timetracking
    restart: unless-stopped
    ports:
      - "4173:3000"
    volumes:
      - ${TIMETRACKING_DATA_DIR:-./data}:/data
